{
  "name": "@omcc/mcp-server",
  "private": true,
  "version": "0.1.0",
  "description": "OMCC MCP server — shared state for agents (PRD/stories, workflow phase, memory, model routing).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "omcc-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^22.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
